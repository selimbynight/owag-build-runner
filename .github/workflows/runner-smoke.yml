name: runner-smoke

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Vérifier la disponibilité du runner
        run: echo "OWAG public runner available"
